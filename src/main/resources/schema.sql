-- =============================================================================
-- VMetrix Query Manager — Schema Definition
-- H2 Database (Oracle Compatibility Mode)
-- =============================================================================

-- Placeholder: metadata and business tables will be defined in subsequent tickets.
-- This file must contain at least one valid SQL statement for Spring Boot init.
SELECT 1;
