package com.vmetrix.querymanager.domain.model;

/**
 * Composite pattern marker interface for filter tree nodes.
 * Implemented by {@link FilterGroup} (composite) and {@link FilterCondition} (leaf).
 */
public interface FilterNode {
}
